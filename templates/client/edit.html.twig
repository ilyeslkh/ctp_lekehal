{{ form_start(form) }}
    <div class="mb-4">
        {{ form_label(form.name, null, {'class': 'block text-sm font-bold'}) }}
        {{ form_widget(form.name, {'attr': {'class': 'border border-gray-300 rounded px-3 py-2 w-full'}}) }}
        {{ form_errors(form.name) }}
    </div>
    <div class="mb-4">
        {{ form_label(form.email, null, {'class': 'block text-sm font-bold'}) }}
        {{ form_widget(form.email, {'attr': {'class': 'border border-gray-300 rounded px-3 py-2 w-full'}}) }}
        {{ form_errors(form.email) }}
    </div>
    <div class="mb-4">
        {{ form_label(form.phone, null, {'class': 'block text-sm font-bold'}) }}
        {{ form_widget(form.phone, {'attr': {'class': 'border border-gray-300 rounded px-3 py-2 w-full'}}) }}
        {{ form_errors(form.phone) }}
    </div>
    <button class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600">Enregistrer</button>
{{ form_end(form) }}
