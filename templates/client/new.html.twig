{% extends 'base.html.twig' %}

{% block title %}Ajouter un Client{% endblock %}

{% block body %}
    <div class="bg-white shadow rounded-lg p-6">
        <h2 class="text-2xl font-bold mb-6 text-gray-800">Ajouter un Client</h2>
        {% include 'client/_form.html.twig' %}
        <a href="{{ path('client_index') }}" class="inline-block text-blue-500 hover:underline mt-4 block">Retour à la liste</a>
    </div>
{% endblock %}
