{{ form_start(form) }}
    <div class="mb-4">
        {{ form_label(form.content, null, {'class': 'block text-sm font-bold mb-1'}) }}
        {{ form_widget(form.content, {'attr': {'class': 'border border-gray-300 rounded px-3 py-2 w-full'}}) }}
        {{ form_errors(form.content) }}
    </div>
    <div class="mb-4">
        {{ form_label(form.client, null, {'class': 'block text-sm font-bold mb-1'}) }}
        {{ form_widget(form.client, {'attr': {'class': 'border border-gray-300 rounded px-3 py-2 w-full'}}) }}
        {{ form_errors(form.client) }}
    </div>
    <button class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600">Enregistrer</button>
{{ form_end(form) }}
